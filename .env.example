# GCP project and deployment
PROJECT_ID=your-gcp-project
REGION=us-central1
JOB_NAME=tensorstore-dvid-export

# Source: GCS path containing DVID Arrow IPC shard files.
# Should contain per-scale directories: s0/, s1/, ..., each with .arrow + .csv pairs.
# Example: gs://mybucket/dvid-exports/mCNS-98d699/segmentation
SOURCE_PATH=gs://your-bucket/path/to/shard/export

# Destination: GCS path for neuroglancer precomputed output.
# Example: gs://mybucket/v1.0/segmentation/precomputed
DEST_PATH=gs://your-bucket/path/to/precomputed/output

# Neuroglancer spec JSON (same file used for DVID export-shards)
# This is the single source of truth for volume geometry and sharding params.
NG_SPEC_PATH=ng-specs.json

# Which scales to process from DVID export shards (comma-separated).
# Workers scan s{N}/ in the source path for each scale listed here.
SCALES=0,1

# Scales to generate by downsampling the previous scale's data from the
# destination volume (comma-separated). Useful for scales not exported by DVID
# (e.g., if MaxDownresLevel was lower than the number of spec scales).
# These run after all shard-based scales are complete. Leave empty to disable.
DOWNRES_SCALES=

# Cloud Run job settings
TASKS=200
MAX_RETRIES=3
TASK_TIMEOUT=3600s
MEMORY=2Gi
CPU=2
